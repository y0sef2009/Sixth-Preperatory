# إعداد لوحة إدارة السادس الإعدادي

## 1. تشغيل مخطط قاعدة البيانات

1. افتح [SQL Editor](https://supabase.com/dashboard/project/onjhusaslbfwwsqwueje/sql/new).
2. افتح ملف [`supabase/schema.sql`](supabase/schema.sql) من المشروع وانسخ محتواه كاملًا.
3. الصق المحتوى داخل SQL Editor.
4. اضغط **Run** مرة واحدة.
5. يجب أن تظهر رسالة نجاح دون أخطاء.

ينشئ الملف جدول المحتوى، وحاوية ملفات عامة، وسياسات تمنح حساب المدير ذي المعرّف المحدد وحده صلاحية الإضافة والتعديل والحذف. يستطيع الزوار قراءة العناصر المنشورة فقط.

## 2. تعطيل التسجيل العام

1. افتح **Authentication → Providers → Email**.
2. عطّل خيار إنشاء المستخدمين الجدد أو **Allow new users to sign up** إن ظهر.
3. اترك تسجيل الدخول بالبريد وكلمة المرور مفعّلًا.

حتى إن بقي التسجيل مفعّلًا، تمنع سياسات RLS أي حساب آخر من إدارة المحتوى، لكن تعطيله يمنع إنشاء حسابات غير ضرورية.

## 3. دخول لوحة الإدارة

بعد نشر الموقع افتح:

```text
https://sixth-preperat00ry.rdam83837.workers.dev/admin
```

سجّل بالبريد وكلمة المرور اللذين أنشأتهما داخل Supabase. لا يوجد رابط للوحة في التنقل العام.

## 4. إضافة المحتوى

1. اختر نوع المحتوى.
2. اكتب العنوان.
3. اختر المادة وتقسيمها.
4. أضف الوصف والمصدر والبيانات الإضافية.
5. ارفع PDF أو صورة، أو أضف رابطًا خارجيًا.
6. فعّل **نشر المحتوى للطلاب** إذا أصبح جاهزًا، أو اتركه مسودة.
7. اضغط **إضافة المحتوى**.

الملفات المقبولة: PDF وJPG وPNG وWebP، وبحد أقصى 50 ميغابايت للملف.

## قواعد أمنية

- لا تضع `service_role` أو المفتاح السري داخل المشروع.
- لا ترسل كلمة مرور حساب المدير أو قاعدة البيانات لأي شخص.
- المفتاح الموجود في الواجهة هو Publishable key عام؛ تعتمد الحماية على Auth وRLS.
